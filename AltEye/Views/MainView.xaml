<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="AltEye.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AltEye.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:AltEye.Views.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Vertical" Grid.Column="1">
            <Button Content="Load" Command="{Binding LoadCommand}" HorizontalAlignment="Center"/>
            <Slider Minimum="0" SmallChange="0.01" StepFrequency="0.01" LargeChange="0.1" Maximum="5" Value="{Binding ElementName=PcbView, Path=Zoom, Mode=TwoWay}" />
        </StackPanel>
        
        <controls:PcbViewControl x:Name="PcbView" Document="{Binding Document}"/>
    </Grid>
</UserControl>
