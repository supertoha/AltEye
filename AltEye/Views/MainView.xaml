<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="AltEye.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AltEye.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:AltEye.Views.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CommandBar HorizontalAlignment="Left" DefaultLabelPosition="Right">
            <AppBarButton Icon="OpenFile" Label="Open" Command="{Binding LoadCommand}" 
                          HorizontalAlignment="Left" HorizontalContentAlignment="Left"/>
            <CommandBar.SecondaryCommands>
                <AppBarButton Icon="Setting" Label="Settings">
                    <AppBarButton.KeyboardAccelerators>
                        <KeyboardAccelerator Modifiers="Control" Key="I" />
                    </AppBarButton.KeyboardAccelerators>
                </AppBarButton>
            </CommandBar.SecondaryCommands>
        </CommandBar>

        <controls:PcbViewControl Grid.Row="1" Document="{Binding Document}"/>

        <ProgressRing Visibility="{Binding IsProgressVisible}" Grid.Row="1" Width="100" Height="100" IsActive="True"
                      HorizontalAlignment="Center" VerticalAlignment="Center" IsIndeterminate="True"/>
    </Grid>
</UserControl>
